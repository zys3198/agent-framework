# DeepSeek (OpenAI-compatible) API key — https://platform.deepseek.com
# Copy this file to `.env` and fill in your key (`.env` is gitignored).
DEEPSEEK_API_KEY=

# Optional overrides (defaults shown):
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# MODEL=deepseek-chat
# MAX_STEPS=10
# MAX_REPLANS=2
# HOST=127.0.0.1
# PORT=8000
